{
  "name": "cypress-grep-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:open": "npx cypress open",
    "cy:run": "npx cypress run --browser chrome --headless",
    "cy:run:allure": "npx cypress run --browser chrome --headless --env allure=true",
    "report:open":  "allure serve",
    "report:generate":  "allure generate",
    "cy:run:with:report":  "(npm run cy:run || true) && npm run report:open",
    "report":  "npm run report:open"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mmisty/cypress-grep-example.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/mmisty/cypress-grep-example/issues"
  },
  "homepage": "https://github.com/mmisty/cypress-grep-example#readme",
  "dependencies": {
    "@mmisty/cypress-allure-adapter": "^0.10.7",
    "allure-commandline": "^2.24.0",
    "cypress": "^13.3.0"
  }
}
